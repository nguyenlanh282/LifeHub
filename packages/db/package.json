{
  "name": "@lifehub/db",
  "version": "1.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "wrangler d1 execute lifehub_db --local --file=./migrations/0000_initial.sql"
  },
  "dependencies": {
    "drizzle-orm": "^0.30.10"
  },
  "devDependencies": {
    "drizzle-kit": "^0.20.18",
    "typescript": "^5.4.5"
  }
}
